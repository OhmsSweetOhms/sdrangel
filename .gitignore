**/core*
CMakeLists.txt.user*
CMakeUserPresets.json
build*/
debug/
qtbuild/*
sdriq/*
presets/*
LOCAL/*
.cproject
.flatpak-builder/
.project
.pydevproject
.vscode/
.settings/
*.cs
*.orig
*.pro.user
*.rej
.idea/*
rescuesdriq/rescuesdriq
debian/sdrangel/*
debian/sdrangel.substvars
debian/files
debian/sdrangel.debhelper.log
debian/debhelper-build-stamp
obj-x86_64-linux-gnu/*
**/venv*/
*.pyc
.DS_Store
.sonarlint

### Go ###
# Binaries for programs and plugins
/rescuesdriq/*.exe
/rescuesdriq/*.exe~
/rescuesdriq/*.dll
/rescuesdriq/*.so
/rescuesdriq/*.dylib

# Test binary, built with `go test -c`
/rescuesdriq/*.test

# Output of the go coverage tool, specifically when used with LiteIDE
/rescuesdriq/*.out

/rescuesdriq/vendor/
/rescuesdriq/Godeps/
/.vs

# WDSP
wdsp/*.o
wdsp/*.h
wdsp/*.c
wdsp/Makefile

### Fork-local (udmabufinput-plugin branch) ###
# Raw wire-capture scratch from plan-03 Steps 3/3b evidence scripts. The
# decoded .txt dumps and evidence-*.md writeups are the committed proof;
# these .bin captures are reproducible via step3_launch_5band.sh + the
# per-port spectrum_ws_capture.py/spectrum_frame_decode.py invocations.
plugins/samplesource/udmabufinput/scripts/step3/evidence/**/*.bin
